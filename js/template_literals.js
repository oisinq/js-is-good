const myName = "Oisín";

// If you want to concat strings normally, you need to do...
let welcomeMessage = "Hello, " + myName + "."; // "Hello, Oisín."

// But with string literals we can do...
welcomeMessage = `Hello, ${myName}`; // Hello, Oisín.

`Using backticks, we can also easily 
add
whitespace in a natural way.`;
/*  
Using backticks, we can also easily 
add
whitespace in a natural way.
*/

export {};
